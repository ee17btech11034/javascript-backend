# Backend using JS:


## Revision:
    -- Basic information about Backend with JS 
    -- Deploy Methods (Module/common js way was wrong in previous for import )



## Current commit details:
    

    -- Data Modeling:
            -- Data store:
                -- first question must be: what we are going to store. Based on this we decide DB.
                --   Moon Modeler               is data modeling tool 
                            -> defines the structure of data.
                            -> create diagram and it will generate the code
                            -> Alternative is       Eraser.io      
                -- mongoose, Prisma helps us in Data modeling
    
    -- We can use code Sandbox, code space by github, stackblitz, replit etc. for online code in machines.


    -- Lets create some models for Data.                             `npm i mongoose`
        -- First we will create Models/todos
            --      use   user.model.js   than       user.js file
            --      user  user.controller.js     than     user.js 

        -- 3 steps to use mongoose:
            -- 1. import mongoose
            -- 2. Create a Schema
            -- 3. Create a model and export it
        
        -- Define data fields in Schemas.

    # we are trying to create a Todo app
        -- user, todo, subtodos will be there. 
        -- Main todo will have a name and subtodo. "Gym', "playlist" as main todos and songs as subtoos
        -- We will give reference to other models as well. 
            -- like todo is created by which user. What will be the relation between todos and subtodos.
        
        -- when setting relation then 2 things to keep in mind:
            -- 1. type: mongoose.Schema.Types.ObjectId    // tells that this is special type
                    -- mongoose me Schemas jo banaye hai unke Types me ObjectId hai
            -- 2. ref: "User"     // DB name which was given in model
                    -- us Id ka ref ditya hai. This is my way of explaining