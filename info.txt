# Backend using JS:


## Revision:
    -- Basic information about Backend with JS 
    -- Deploy Methods (Module/common js way was wrong in previous for import )



## Current commit details:
    

    -- Data Modeling:
            -- 1. Ecommerce:
                -- First we should create fileds or models which are independent
                    -- User model create kro
                -- Than jo ek par dependent hai and then 2 par, 3 par dependent hai, so on...
                    -- Category model kro    --> Mongoose     categories   hi banata hai plural me
                        -- Agar ham  "categories" banate hai hai model me to wo kuch nhi krta isi name se DB  collection banata hai.  Inteligent hai check krta hai ki plural hai ki nhi.
                        -- Model me given name ko hi model name bhi bolna chahiye STandard approcah hai.

                        -- // Avoid recompiling the model if already exists (important in hot-reload environments)
                            export const Category = mongoose.models.Category || mongoose.model('Category', categorySchema);
                                -- If this file is imported multiple times (e.g., in API routes, during development with hot reload), Mongoose will try to register the same model again, leading to:
                                    -- OverwriteModelError: Cannot overwrite 'Category' model once compiled. 
                                        This is a common error in full-stack frameworks like Next.js. 
                    
                    -- product
                        -- we can store images in mongoDB in Buffer format but not recommended as it make DB heavy.
                        -- We can upload images in a folder in backend and then use image path.
                        -- Use third party to store the images/videos and then provide the url:
                            -- Cloudnary, S3 bucket
                    
                    -- order
                        -- 