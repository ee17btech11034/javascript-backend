# Backend using JS:


## Revision:
    -- Basic information about Backend with JS 



## Current commit details:
    

    -- Install Node: Used for runtime JS 
    -- Express: Used to stablish connection between user and server. it handles req and res 


    --  Create Empty node Application:
        -- `npm init`
            -- initialize using node package manager. simile "create react app'
            -- it needs some permissions or provide some options.
            -- to skip all and provide yes to all          `npm init -y`
            -- create    "package.json" file

    -- Create a index.js file
        -- we run this using   `node index.js`
        -- we can create a command for this, like dev, start build command in     package.json > scripts    `"start": "node index.js"`
            -- now we can use        ` npm run start` 

    -- setup 'express'
        -- install `npm install express
        -- Create a express app in index.js file : https://expressjs.com/ 
        
        -- we will have to restart app to "HOT Reload (auto compiled and served, restart)" 
            -- whenever there is any change then we will have to manually restart this. 
            -- For "Hot reloading" use Nodemon
        
    -- When we go in production then we are not sure which port we will get. 
        -- install       `npm i dotenv`
        -- create .env file


    -- Where can we deploy:
        -- We can deploy on AWS, Azure, DigitalOcean, etc.
        -- Digital Ocean, Heroku, railway, seenode, render, cyclic.sh, etc for minimum amount like 3-5 $/month
        -- Upload on git. (We can access public and private both)

        -- Digital ocean:
            -- Create > App (can be done in droplet but not recommended)
            -- select repo > branch: main  > src dir: /
            -- Edit plan > Basic 
            -- Environment: 
                -- App is a big container and can have multiple mini apps.
                -- Global > PORT: 3000 but it will be provided to service app is not necessary
                -- Local > PORT:3000 (override to give this 3000 port)
                    -- if this port is not available then it will override this as well with available port 
            
            -- click on app url after complition:
                -- we can change this with "custom domain' 
