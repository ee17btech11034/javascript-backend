# Backend using JS:


## Revision:
    -- Basic information about Backend with JS 
    -- Deploy Methods (Module/common js way was wrong in previous for import )
    -- Ecommerce, Hospital data modelling


# project: https://app.eraser.io/workspace/YtPqZ1VogxGy1jzIDkzj
            https://www.youtube.com/watch?v=9B4CvtzXRpc&list=PLu71SKxNbfoBGh_8p_NS-ZAh6v7HhYqHW&index=7 


# `.gitkeep` file in empty folder to track that
# `.gitignore` file to ignore file:
    -- can use gitignore generator.

## Current commit details:
    -- Photos/videos Store:
        -- Better approach is:  We store it on our backend first for temorarily, then we upload it on Couldnary, S3, etc. 


## Project progress:
    -- `npm init`
    -- add gitignore file generated by 'gitignore online generator'
    -- create files   
            CMD     src >     `touch  app.js 
                                        constants.js 
                                        index.js`  one by one
    -- package.json :==>           type: "module"
    -- server stop and start on each changes is necessary:
        -- New option     `...watch`  is added in nodeJs, it continiously watch the changes. But still not using in production, right now,
        -- `nodemon` utility can do this        
            -- install as dev dep:    `npm i -D nodemon`  
            -- crweate a  new script as dev with nodemon run:    `"dev": "nodemon src/index.js"`
            -- `npm run dev` to run first time.

    -- Create folders in src:
        -- controllers  --> functions
        -- db       --> DB connections
        -- middlewares  --> in between code in req, cookies, check or verify user, etc
        -- models  --> DB Schemas, models 
        -- routes --> all routes 
        -- utils --> utilities like file upload, mailing, etc 

    -- Prettier tool ==> helps us in formating our code so whole team is on same page like tab is 2 space or 4, etc.
        -- `npm i -D prettier`
        -- create a file   `.prettierrc`
        -- `.prettierignore` file

    -- MongoDB Atlas:
        -- Sub service of mongoDB to provide online Database
        -- steps:
            -- Sign in > org name (Raja's ord)
            -- 1. if new:
                    New Project >  proj name > create 
                    create deployment > aws provider > Mumbai region > cluster name can be anything > create
                    Security Quickstart > Username & pass (no special characters)  >  "my local environment" > IP (0.0.0.0/0) > add entry > finish 
            -- 2. Edit existing project:
                    Go to security >    
                        > Network Access > Delete ip and add ip (ip of machine which has Backend code) > 0.0.0.0/0 means access to all machines just for testing
                        > Db access > Add new user > uname & pass > builtin role (read and write in any)
            -- Go to Deployment > Database
                    -- click on  'connect' (beside cluster name) > can choose any here we will use "compass" 
                        > copy the connection string > password must be replaced when paste in env file
                    

    -- Go to .env file 
            -- paste that connection string in here 
            -- it should not have "/" in the end of string.
    
    -- constants.js file:
            -- define and export variables which are not system specific but important
    
    -- DB connection:
            -- Either we can write all code in index.js or create inside DB folder. 
                    -- use IFEE   `;(async()=>{})()`
            -- Better approach is to write seperatelly.
        
    -- ` npm i mongoose dotenv express`

    -- DB:
            -- DB can have issues so wrap inside try-catch.
            -- DB is in another continent so use async-await.


    -- app.js:
            -- create app and listen.
            -- request:
                -- req.params ==> it contains data we get from url.
                -- req.body ==> it contains data we get from forms, etc.

            -- middlewares:
                -- `npm i cookie-parser`
                -- `npm i cors`   => Cross Origin Resource Sharing

            -- there are 4 params: 
                -- (error, req, res, next)
                    -- next for middlewares
    
    -- utility:
            -- Here whenever we are tring to talk to DB (in UserController, other controllers)
                -- we will be using a piece of code, create a file in utils for that
                -- Read about Higher Order Function.
    -- Error api class:
            -- it tells me to standerdize my error logs 
            -- check in utils

            -- We created this class bcz Node handles req, it provide this.

    -- But we send res using express not Node. 
        -- Express does not provide anything direct but we can use cookies.

    -- Status Code:
        --

    -- Mongo DB Models:
        -- it stores data in BSON format not JSON.
        -- user model:
            -- Created User Model then Video Model 
            -- watch History is important. We can use basic insert and pop methods. 
                -- But better to use  a package:    " mongoose-aggregate-paginate-v2"
                    -- `npm i mongoose-aggregate-paginate-v2`
                    -- it allows us to write aggregation queries. it is just a plugin
                    -- Use this instead of "insertmany", "updateMany", etc.

        -- bcrypt vs bcryptjs:
            -- bcrypts is aNodeJs library package. 
            -- bcryptjs is optimized in JS with Zero dep and compaible with bcrypt.
            -- we can use any but here we will be using `bcrype`
                -- `npm i bcrypt`
                -- helps us to encrypt password.
        
        -- Tokens:
            -- provide tokens like JWT, etc.
                -- `npm i jasonwebtoken`
                -- JWT is bearer token means whoever has this is correct person.
                -- We need "Access token secret" to generate JWT. 
                    -- algos to generate these secrets are "Shard256, or andon string generate.
        
        -- We directly can not use jwt or entryption in model. 
            -- we need to use Hooks/middlewares, like pre (do reight before storng the data)

        -- We need methods which compare users password.

        -- Tokens in env:
                ACCESS_TOKEN_SECRET=nasiwnkwjcnwkjcwjdnwd
                ACCESS_TOKEN_EXPIRY=1d // have less timelimit
                REFRESH_TOKEN_SECRET=cwsjdbcjnc
                REFRESH_TOKEN_EXPIRY=10d // have more time limit than ACCESS_TOKEN_EXPIRY 

                -- Here we will be using both "Sessions" and "Cookies"
                -- Access token is not stored in DB but we store Refresh token in DB.
                
        -- file upload:
                --  We generally do not upload file on our server, we use third party.
                -- We create a file upload function in utility and then we can reuse it for video, picture, pdf, etc upload.
                -- We generally set this in middleware.
                -- Cloudinary
                -- we need packages  `express-fileupload" or "multer"
                    -- Noadays people use Multer.
                
                ## Multer:
                    -- `npm i cloudinary multer`

                ## Cludinary: 
                    -- We will make user to upload file using Multer and then we will store it on cloudinary.
                    